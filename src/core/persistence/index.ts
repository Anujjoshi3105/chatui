export {
  loadMessages,
  saveMessages,
  clearMessages,
  getThreadMessagesKey,
  loadCurrentThreadId,
  saveCurrentThreadId,
} from "./storage-adapter"
export type { PersistedMessage } from "./storage-adapter"
